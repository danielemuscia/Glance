import CoreGraphics

enum Radius {
    /// 6 — secondary buttons, separator inset
    static let sm: CGFloat = 6

    /// 8 — row highlight, MenuItemStyle default, peek row hover
    static let md: CGFloat = 8

    /// 10 — primary CTA, conflict card
    static let lg: CGFloat = 10
}
